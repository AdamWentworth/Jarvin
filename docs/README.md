# Jarvin Docs

This folder is the human-facing reference layer for the repo.

Use these docs first:

- `architecture.md`: the runtime path, major modules, and system invariants.
- `runbook.md`: setup, run, diagnose, and test commands.

Agent-specific notes live under `.agents/`.
